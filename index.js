const light = document.getElementById(on);
const dark = document.getElementById(off);

on.addEventListener("submit", function (event) {
    
}

)

orm.addEventListener("submit", function (event) {
    event.preventDefault();